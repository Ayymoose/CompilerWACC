# Expected:

begin
  class Animal open
    int numberOfLegs,
    bool isFast
  close

  Animal cheetah = new Animal(4, true);
  if cheetah.isFast then
    print "This animal is FAST!"
  else skip
  fi

end
